import { ElementMovementDirective } from "./element-movement.directive";

describe("ElementMovementDirective", () => {
    it("should create an instance", () => {
        const directive = new ElementMovementDirective();
        expect(directive).toBeTruthy();
    });
});
